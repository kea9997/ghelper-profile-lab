
'use strict';
const TOKEN='__SESSION_TOKEN__';
const $=id=>document.getElementById(id);
const pages={profiles:'내 설정',benchmark:'성능 비교',community:'설정 자료실'};
let state=null,stopped=false,pollTimer=null,refreshing=false,dialog=null,dialogId=0,toastTimer=null;
let updateInfo=null,updateChecked=false,updateChecking=false;
let selectedPublic='',publicSelection=new Set(),cache={},operationPending=false,setupExpanded=false;
let catalogVisible=18;
const modeName=v=>({0:'균형',1:'터보',2:'조용'})[v]||'모드 정보 없음';
const number=v=>typeof v==='number'&&Number.isFinite(v)?v.toLocaleString('ko-KR'):'—';
const scoreSummary=r=>'그래픽 '+number(r.graphicsScore)+'점 · CPU '+number(r.cpuScore)+'점';
function referenceScore(t){const e=node('p',null,'reference-score');e.append(node('strong','그래픽 '+(t.graphics==null?'미공개':number(t.graphics)+'점')),node('span',' · CPU '+(t.cpu==null?'미공개':number(t.cpu)+'점'),'small muted'));return e;}
const date=v=>v?new Date(v).toLocaleString('ko-KR',{month:'numeric',day:'numeric',hour:'2-digit',minute:'2-digit'}):'날짜 없음';
const node=(tag,text,cls)=>{const e=document.createElement(tag);if(text!=null)e.textContent=String(text);if(cls)e.className=cls;return e;};
const button=(text,action,cls='btn secondary')=>{const e=node('button',text,cls);e.type='button';e.addEventListener('click',action);return e;};
const notice=(text,kind='')=>node('div',text,'notice '+kind);
const pill=(text,kind='')=>node('span',text,'status-pill '+kind);
function detail(title,content,key){const d=node('details');if(key)d.dataset.key=key;d.append(node('summary',title),content);return d;}
function detailsList(entries){const d=node('dl',null,'details-list');for(const [k,v]of entries)d.append(node('dt',k),node('dd',v??'확인되지 않음'));return d;}
function safeUrl(value){try{const u=new URL(value);return u.protocol==='https:'?u.href:null;}catch{return null;}}
function sameSettings(p){const c={...state.config};delete c.performance_mode;const keys=Object.keys(c);return keys.length===Object.keys(p.settings||{}).length&&keys.every(k=>c[k]===p.settings[k]);}
function isActive(){return ['running','cooling','waiting','restoring','awaiting_stop'].includes(state?.benchmark?.status);}
function showToast(text,error=false){clearTimeout(toastTimer);$('toast-text').textContent=text;$('toast').classList.toggle('error',error);$('toast').hidden=false;toastTimer=setTimeout(()=>$('toast').hidden=true,error?10000:5000);}
function report(error){showToast(error.message||String(error),true);}
async function api(path,body,blob=false){const r=await fetch(path,{method:body===undefined?'GET':'POST',headers:{'X-Session-Token':TOKEN,...(body===undefined?{}:{'Content-Type':'application/json'})},body:body===undefined?undefined:JSON.stringify(body),credentials:'same-origin',redirect:'error',cache:'no-store'});if(!r.ok){let msg='처리하지 못했습니다. 다시 시도해 주세요.';try{msg=(await r.json()).error||msg;}catch{}throw Error(msg);}return blob?r.blob():r.json();}
async function act(btn,action){if(operationPending)return;operationPending=true;const label=btn.textContent;btn.disabled=true;btn.textContent='잠시만요…';try{await action();}catch(e){report(e);}finally{operationPending=false;btn.textContent=label;btn.disabled=false;renderAvailability();}}
function navigate(){let p=location.hash.slice(1);if(p==='laptop')p='profiles';if(!pages[p])p='profiles';document.querySelectorAll('[data-section]').forEach(e=>e.hidden=e.dataset.section!==p);document.querySelectorAll('[data-page]').forEach(e=>{if(e.dataset.page===p)e.setAttribute('aria-current','page');else e.removeAttribute('aria-current');});document.title=pages[p]+' · GHelper Profile Lab';if(p==='community')scheduleLibraryLoad();}
function accept(s){if(!s?.hardware||!s?.discovery)throw Error('노트북 정보를 읽지 못했습니다.');state=s;render();$('loading').hidden=true;$('app-content').hidden=false;$('connection-error').hidden=true;$('sync-status').textContent='최근 확인 '+new Date().toLocaleTimeString('ko-KR');if(s.updateStatus==='updated')showToast('업데이트가 완료됐어요. 현재 버전은 v'+s.appVersion+'입니다.');else if(s.updateStatus==='failed')showToast('자동 업데이트에 실패했어요. 기존 버전으로 다시 시작했습니다. GitHub 릴리스에서 직접 설치해 주세요.',true);if(!updateChecked){updateChecked=true;checkForUpdates(false);}}
function schedule(){clearTimeout(pollTimer);if(!stopped)pollTimer=setTimeout(()=>refresh(false,true),isActive()?2500:15000);}
async function refresh(force=false,silent=false){if(refreshing||stopped)return;refreshing=true;try{accept(await api(force?'/api/refresh':'/api/state',force?{}:undefined));}catch(e){$('loading').hidden=true;$('connection-error').textContent='프로그램과 연결이 끊겼습니다. 실행 파일을 다시 열어 주세요.';$('connection-error').hidden=false;if(!silent)report(e);}finally{refreshing=false;schedule();}}
function memo(id,value,draw){const v=JSON.stringify(value);if(cache[id]===v)return;cache[id]=v;const target=$(id),opened=new Set([...target.querySelectorAll('details[open][data-key]')].map(e=>e.dataset.key));target.replaceChildren();draw(target);target.querySelectorAll('details[data-key]').forEach(e=>{if(opened.has(e.dataset.key))e.open=true;});}
function render(){renderHeader();renderCurrentVisual();renderProfiles();renderBackups();renderBenchmark();renderRuns();renderCommunity();renderLibrary();if(location.hash==='#community')scheduleLibraryLoad();renderCatalog();renderSettings();renderAvailability();}
function renderHeader(){const d=state.discovery;let model=state.hardware.model||'내 노트북';model=model.replace(/^ROG /,'').replace(/_(GU|GA|GZ)\w+$/,'');$('device-summary').textContent=model+' · 현재 '+modeName(state.config?.performance_mode);$('connection-status').textContent=d.config_verified?'설정 연결됨':'연결 확인 필요';$('connection-status').className='status-pill '+(d.config_verified?'good':'warn');$('app-version').textContent='v'+(state.appVersion||'확인 불가');$('update-button').textContent=updateInfo?.available?'새 버전 v'+updateInfo.latestVersion:'업데이트 확인';$('update-button').classList.toggle('has-update',!!updateInfo?.available);let hint='';if(!d.config_verified)hint='설정을 찾지 못했어요. 위의 「도움말 · 설정」에서 연결을 확인해 주세요.';else if(isActive())hint='성능을 비교하는 중입니다. 끝난 뒤 설정을 저장하거나 적용할 수 있어요.';$('home-hint').hidden=!hint;$('home-hint').textContent=hint;}
async function checkForUpdates(showDialog=true){if(updateChecking)return;updateChecking=true;const btn=$('update-button');btn.disabled=true;const previous=btn.textContent;btn.textContent='확인 중…';try{updateInfo=await api('/api/update/check');if(state)renderHeader();if(showDialog)showUpdateDialog(updateInfo);}catch(e){btn.textContent=previous;if(showDialog)report(e);}finally{updateChecking=false;btn.disabled=false;}}
function showUpdateDialog(info){const body=node('div',null,'stack');body.append(node('p','현재 v'+info.currentVersion+' · 최신 v'+info.latestVersion));if(info.available){body.append(node('p','새 버전을 내려받아 확인한 뒤 프로그램을 다시 시작합니다. 저장한 설정과 점수는 유지됩니다.'));if(info.notes)body.append(node('p',info.notes,'small muted update-notes'));if(isActive())body.append(notice('성능 비교를 마친 뒤 업데이트할 수 있습니다.'));}else body.append(node('p','현재 최신 버전을 사용 중입니다.'));const link=node('a','GitHub 릴리스 보기 ↗','btn secondary');link.href=safeUrl(info.releaseUrl)||state.releaseUrl;link.target='_blank';link.rel='noopener noreferrer';body.append(link);openModal(info.available?'업데이트가 있습니다':'최신 버전입니다',body,info.available&&!isActive()?'업데이트 설치':'',info.available&&!isActive()?installUpdate:null);}
async function installUpdate(){const result=await api('/api/update/install',{});stopped=true;clearTimeout(pollTimer);$('modal-body').replaceChildren(notice(result.message||'앱을 다시 시작합니다.'));$('modal-confirm').hidden=true;return false;}
function driver(){return document.querySelector('input[name=driver]:checked').value;}
function renderAvailability(){if(!state)return;const busy=isActive()||operationPending,d=state.discovery;for(const id of ['quick-save','import-button','scan-results'])$(id).disabled=busy||(id==='quick-save'&&!d.config_verified);$('capture-form').querySelector('button').disabled=busy||!d.config_verified;$('config-form').querySelector('button').disabled=busy;$('start-benchmark').disabled=busy||!d.config_verified||!d.ghelper_running||!(driver()==='enterprise'?d.cli_exe:d.benchmark_exe);$('cancel-benchmark').disabled=!isActive()||operationPending;$('prepare-community').disabled=!(state.profiles||[]).length||operationPending;document.querySelectorAll('[data-apply]').forEach(e=>e.disabled=busy||e.dataset.same==='true');}
function defaultName(){return '내 설정 · '+new Date().toLocaleString('ko-KR',{month:'numeric',day:'numeric',hour:'2-digit',minute:'2-digit'});}
async function saveCurrent(name='',notes=''){const p=await api('/api/profiles/capture',{name:name.trim()||defaultName(),notes:notes.trim()});await refresh();showToast('「'+p.name+'」을 저장했어요. 아래 목록에서 다시 쓸 수 있습니다.');}
function renderProfiles(){const all=state.profiles||[];$('profile-count').textContent=all.length+'개';memo('profile-list',[all,state.config,state.runs?.map(r=>[r.settingsHash,r.status])],target=>{if(!all.length){const e=node('div',null,'empty');e.append(node('strong','아직 저장한 설정이 없어요'),node('p','지금 쓰는 설정이 마음에 든다면 「현재 설정 저장」을 눌러 두세요.'));target.append(e);return;}const normal=all.filter(p=>p.notes!=='벤치마크 시작 시 자동 저장'),auto=all.filter(p=>p.notes==='벤치마크 시작 시 자동 저장');for(const p of normal)target.append(profileRow(p));if(auto.length){const list=node('div',null,'stack');auto.forEach(p=>list.append(profileRow(p)));target.append(detail('성능 비교 중 자동 저장한 설정 · '+auto.length+'개',list,'auto-profiles'));}});}
function profileRow(p){const row=node('article',null,'saved-row'),main=node('div',null,'saved-main'),title=node('div',null,'row');title.append(node('h3',p.name));const same=sameSettings(p);if(same)title.append(pill('지금 설정과 같음','good'));main.append(title,node('p',date(p.createdAt)+(p.origin==='imported'?' · 가져온 설정':' · 직접 저장'),'saved-meta'));if(p.notes&&p.notes!=='벤치마크 시작 시 자동 저장')main.append(node('p',p.notes,'small muted'));const actions=node('div',null,'saved-actions');const apply=button(same?'현재 설정':'적용하기',()=>act(apply,()=>openApply(p)),same?'btn secondary':'btn primary');apply.dataset.apply=p.id;apply.dataset.same=String(same);actions.append(apply);const more=node('div',null,'stack');more.append(detailsList([['노트북',p.hardware?.model],['포함된 모드',[2,0,1].filter(m=>Object.keys(p.settings||{}).some(k=>k.endsWith('_'+m))).map(modeName).join(' · ')||'확인 필요']]));const file=button('파일로 내보내기',()=>act(file,async()=>download(await api('/api/profiles/export?id='+encodeURIComponent(p.id),undefined,true),fileName(p.name)+'.json')));more.append(file,detail('기술 정보',node('pre',JSON.stringify({settings:p.settings,settingsHash:p.settingsHash},null,2),'code')));const visual=button('설정 보기',()=>showSavedSettings(p),'btn text');main.append(visual,detail('파일·세부 정보',more,'profile-'+p.id));row.append(main,actions);return row;}
const settingNames={limit_total:'CPU 총 전력',limit_slow:'CPU 지속 전력',limit_fast:'CPU 순간 전력',limit_cpu:'CPU 전력',auto_apply:'팬 설정 자동 적용',auto_apply_power:'전력 설정 자동 적용',auto_boost:'CPU 부스트',performance:'Windows 전원 설정',gpu_temp:'GPU 목표 온도',gpu_power:'GPU 전력 설정',gpu_boost:'GPU 부스트',gpu_core:'GPU 코어 클럭',gpu_memory:'GPU 메모리 클럭',gpu_clock_limit:'GPU 최대 클럭'};
function settingLabel(key){const m=key.match(/^(.*)_([012])$/);if(!m)return key;let label=settingNames[m[1]];if(!label&&m[1].startsWith('fan_profile_'))label=({cpu:'CPU 팬',gpu:'GPU 팬',mid:'보조 팬'})[m[1].slice(12)]+' 곡선';return modeName(m[2])+' · '+(label||m[1]);}
function settingValue(key,value){if(value==null)return '기본값 사용';if(key.startsWith('fan_profile_'))return '사용자 지정 곡선';if(/^auto_apply/.test(key))return value?'켜짐':'꺼짐';return String(value);}
function changesView(changes){const table=node('table'),head=node('tr');['바뀌는 설정','지금','적용 후'].forEach(t=>head.append(node('th',t)));const thead=node('thead');thead.append(head);table.append(thead);const body=node('tbody');for(const c of changes){const tr=node('tr');tr.append(node('td',settingLabel(c.key)),node('td',settingValue(c.key,c.before)),node('td',settingValue(c.key,c.after)));body.append(tr);}table.append(body);const wrap=node('div',null,'table-wrap');wrap.append(table);return detail('바뀌는 항목 '+changes.length+'개 확인',wrap);}
async function openApply(p){accept(await api('/api/state'));const preview=await api('/api/profiles/preview',{id:p.id}),body=node('div',null,'stack');body.append(node('p','「'+p.name+'」을 사용할 준비를 합니다.'));if(!preview.compatible){body.append(notice('이 노트북에 맞지 않는 설정이라 적용할 수 없어요.','warn'));for(let r of preview.reasons||[])body.append(node('p',r.replace(/^model /,'노트북 기종 ').replace(/^cpu /,'CPU ').replace(/^gpu /,'그래픽카드 '),'small'));openModal('다른 설정을 선택해 주세요',body);return;}if(isActive()){body.append(notice('진행 중인 성능 비교가 끝난 뒤 다시 시도해 주세요.'));openModal('지금은 비교 중이에요',body);return;}const changes=preview.changes||[];if(!changes.length){body.append(notice('지금 사용 중인 설정과 같아서 바꿀 필요가 없어요.','good'));openModal('이미 같은 설정이에요',body);return;}if(state.discovery.ghelper_running){body.append(notice('G-Helper를 먼저 종료해 주세요. 설정을 저장하려면 잠시 꺼야 합니다.','warn'));const steps=node('ol');steps.append(node('li','시계 옆 G-Helper 아이콘을 우클릭하고 「종료」를 선택하세요.'),node('li','종료한 뒤 아래 버튼을 눌러 주세요.'));body.append(steps,changesView(changes));openModal('먼저 G-Helper를 종료해 주세요',body,'종료했어요 · 다시 확인',async()=>{await openApply(p);return false;});return;}body.append(notice('지금 설정은 자동으로 백업됩니다. 나중에 「이전 설정으로 되돌리기」에서 복원할 수 있어요.','good'),changesView(changes));openModal('이 설정을 적용할까요?',body,'이 설정 적용',async()=>{await api('/api/profiles/apply',{id:p.id,configHash:preview.configHash});await refresh();const done=node('div',null,'stack');done.append(node('p','설정을 저장했습니다. G-Helper를 다시 실행하면 반영됩니다.'),notice('문제가 있으면 내 설정 → 이전 설정으로 되돌리기를 이용하세요.'));openModal('G-Helper를 다시 실행해 주세요',done);return false;});}
function renderBackups(){const all=state.backups||[];$('backup-count').textContent=all.length?' · '+all.length+'개':'';memo('backup-list',all,target=>{if(!all.length){target.append(node('p','설정을 적용하면 그 직전 상태를 자동으로 보관합니다.','muted small'));return;}[...all].sort((a,b)=>String(b.createdAt).localeCompare(String(a.createdAt))).forEach((b,i)=>{const row=node('div',null,'row between');row.append(node('span',(i===0?'가장 최근 · ':'')+date(b.createdAt)),button('이때로 되돌리기',()=>openRestore(b)));target.append(row);});});}
async function openRestore(b){try{accept(await api('/api/state'));const body=node('div',null,'stack');body.append(node('p',date(b.createdAt)+'의 백업으로 전체 G-Helper 설정을 되돌립니다.'));if(isActive()){body.append(notice('성능 비교가 끝난 뒤 다시 시도해 주세요.'));openModal('지금은 비교 중이에요',body);return;}if(state.discovery.ghelper_running){body.append(notice('시계 옆 G-Helper 아이콘을 우클릭 → 종료한 뒤 다시 확인해 주세요.','warn'));openModal('먼저 G-Helper를 종료해 주세요',body,'종료했어요 · 다시 확인',async()=>{await openRestore(b);return false;});return;}openModal('이전 설정으로 되돌릴까요?',body,'되돌리기',async()=>{await api('/api/backups/restore',{id:b.id});await refresh();openModal('복원했어요',node('p','G-Helper를 다시 실행하면 이전 설정이 반영됩니다.'));return false;});}catch(e){report(e);}}
function renderBenchmark(){const b=state.benchmark||{},d=state.discovery,active=isActive(),n=Number(b.completed)||0;const hasResults=(state.runs||[]).length>0;$('benchmark-setup').hidden=active||(hasResults&&!setupExpanded);$('prepare-again').hidden=active||!hasResults;$('prepare-again').textContent=setupExpanded?'준비 안내 접기':'새로 비교하기';$('benchmark-progress-panel').classList.toggle('finished',!active);$('benchmark-progress-panel').hidden=b.status==='idle'||!b.status;$('driver-note').textContent=driver()==='steam'?'Steam판 3DMark에서 Time Spy 기본 테스트를 자동으로 선택하고 실행합니다.':driver()==='guided'?'모드는 자동으로 바뀝니다. 각 모드에서 Time Spy의 실행 버튼을 직접 눌러 주세요.':'3DMark for Enterprise의 공식 자동 실행 기능을 사용합니다.';let ready='충전기를 연결하고 G-Helper를 켠 상태에서 시작하세요.';if(!d.config_verified)ready='「도움말 · 설정」에서 G-Helper 연결을 먼저 확인해 주세요.';else if(!d.ghelper_running)ready='G-Helper를 실행한 뒤 오른쪽 위의 새로고침을 눌러 주세요.';else if(driver()==='enterprise'&&!d.cli_exe)ready='Enterprise 실행 파일을 찾지 못했습니다. Steam판 자동 실행을 선택하세요.';else if(!(driver()==='enterprise'?d.cli_exe:d.benchmark_exe))ready='3DMark 설치를 확인한 뒤 오른쪽 위의 새로고침을 눌러 주세요.';$('benchmark-readiness').textContent=ready;const names={idle:'준비',running:'준비 중',cooling:'잠시 대기',waiting:'측정 중',restoring:'마무리 중',awaiting_stop:'중단 대기',complete:'비교 완료',completed:'비교 완료',cancelled:'중단 완료',failed:'중단됨',recovery:'모드 확인 필요'};$('benchmark-status').textContent=b.status==='waiting'&&!b.engineRunning&&(b.driver||driver())==='guided'?'실행 대기':names[b.status]||'상태 확인';$('benchmark-count').textContent=n+' / 3 완료';$('benchmark-message').textContent=b.message||'';$('benchmark-fill').style.width=(Math.min(3,n)/3*100)+'%';$('benchmark-progress').setAttribute('aria-valuenow',String(n));$('mode-steps').replaceChildren();[2,0,1].forEach((m,i)=>{const e=node('div',null,'mode-step '+(i<n?'done':active&&String(b.mode)===String(m)?'current':''));e.append(node('strong',modeName(m)),node('span',i<n?'완료':active&&String(b.mode)===String(m)?'진행 중':'대기'));$('mode-steps').append(e);});let title='준비가 되면 비교를 시작하세요.',text='';if(b.status==='cooling'){title='잠시 기다려 주세요';text='모드가 바뀌었습니다. 노트북이 식도록 기다린 뒤 3DMark를 엽니다.';}else if(b.status==='waiting'){const guided=(b.driver||driver())==='guided';title=guided&&!b.engineRunning?'3DMark에서 Time Spy를 실행해 주세요':b.engineRunning?'Time Spy를 측정하고 있어요':'Time Spy 자동 실행 중이에요';text=guided&&!b.engineRunning?'Time Spy 기본 테스트의 실행 버튼을 누르세요. 점수는 자동으로 모읍니다.':b.engineRunning?'끝날 때까지 기다리세요. 점수를 저장하면 다음 모드로 넘어갑니다.':'3DMark 화면에서 기본 테스트를 선택하고 실행합니다.';}else if(b.status==='running'){title='모드를 준비하고 있어요';text='G-Helper 모드가 바뀌는 동안 잠시 기다려 주세요.';}else if(b.status==='restoring'){title='원래 모드로 돌아가는 중이에요';text='마무리될 때까지 프로그램을 켜 두세요.';}else if(b.status==='awaiting_stop'){title='3DMark에서 테스트를 멈춰 주세요';text='테스트가 멈춘 것을 확인하면 원래 모드로 돌아갑니다.';}else if(['complete','completed'].includes(b.status)){title='세 가지 모드의 비교가 끝났어요';text='아래 점수를 확인하고 소음·팬 속도를 기록해 보세요.';}else if(b.status==='cancelled'){title='비교를 중단했어요';text='시작하기 전 모드로 돌아왔습니다.';}else if(b.status==='failed'){title='비교를 끝내지 못했어요';text=b.message||'문제를 확인한 뒤 다시 시작해 주세요.';}else if(b.status==='recovery'){title='G-Helper에서 원래 모드를 확인해 주세요';text=b.message||'자동으로 돌아오지 못했습니다.';}$('benchmark-next-title').textContent=title;$('benchmark-next-text').textContent=text;$('cancel-benchmark').hidden=!active;$('cancel-benchmark').textContent='비교 중단';renderCountdown();}
function renderCountdown(){if(state?.benchmark?.status!=='cooling')return;const until=state.benchmark.cooldownUntil;if(until){const left=Math.max(0,Math.ceil(until-Date.now()/1000));$('benchmark-status').textContent=left?'대기 '+left+'초':'다음 단계 준비 중';}}
function startBenchmark(){const body=node('div',null,'stack');body.append(node('p','조용 → 균형 → 터보 순서로 실제 Time Spy 테스트를 진행합니다.'),notice(driver()==='steam'?'Steam판 3DMark에서도 앱이 Time Spy를 자동 실행합니다. 실행 중에는 3DMark 창을 닫지 마세요.':driver()==='guided'?'각 모드마다 3DMark에서 실행 버튼을 한 번씩 눌러 주세요.':'3DMark의 공식 자동 실행 기능을 사용합니다. 유효한 실행 권한이 필요합니다.'));body.append(node('p','충전기를 연결해 주세요. 모드마다 2분을 기다리며, 비교 중에는 설정을 바꾸지 마세요.','small muted'));if(state.benchmark?.status==='recovery')body.append(notice('G-Helper에서 원하는 시작 모드로 먼저 바꾼 뒤 진행해 주세요.','warn'));openModal('성능 비교를 시작할까요?',body,'비교 시작',async()=>{const result=await api('/api/benchmark/start',{driver:driver(),cooldownSeconds:120});state.benchmark=result;setupExpanded=false;renderBenchmark();renderAvailability();schedule();});}
function renderRuns(){
  const runs=state.runs||[];
  $('run-count').textContent=runs.length+'회';
  $('past-result-note').hidden=!runs.some(r=>r.binding==='unbound');
  memo('run-list',runs,target=>{
    if(!runs.length){target.append(notice('아직 측정한 점수가 없어요. 비교를 마치면 이곳에 나타납니다.'));return;}
    const sorted=[...runs].sort((a,b)=>String(b.createdAt).localeCompare(String(a.createdAt)));
    const cardData=ScoreCard.build(sorted,state.hardware);
    if(cardData){
      const heading=node('div',null,'result-heading');
      heading.append(node('p','같은 설정으로 측정한 모드별 최신 점수','small muted'));
      const save=button('내 점수 이미지 저장',()=>act(save,async()=>{
        const image=await ScoreCard.png(state.runs,state.hardware);
        download(image.blob,image.fileName);
      }),'btn primary');
      heading.append(save);target.append(heading);
      const grid=node('div',null,'result-grid');
      cardData.modes.forEach(({id,run:r})=>{
        const card=node('article',null,'result-card');
        card.append(node('h3',modeName(id)),node('span','그래픽 점수','small muted'),node('strong',r?number(r.graphicsScore)+'점':'미측정','result-score'),node('p',r?'CPU 점수 '+number(r.cpuScore)+'점':'CPU 점수 —','small'));
        if(r)card.append(node('p',date(r.createdAt),'small muted'),node('p',(r.noiseDbA==null?'소음 미입력':r.noiseDbA+' dBA')+' · '+(r.fanRpm==null?'팬 속도 미입력':number(r.fanRpm)+' RPM'),'small'),button('소음·팬 속도 기록',()=>showRun(r),'btn text'));
        grid.append(card);
      });
      target.append(grid,node('p','PNG에는 기종·CPU·GPU·그래픽·CPU 점수와 직접 입력한 소음·팬 속도만 들어갑니다. 온라인에 자동 게시하지 않아요.','small muted score-caption'));
    }
    const direct=sorted.filter(r=>r.status==='valid'&&r.binding==='unbound');
    if(direct.length){const box=node('div',null,'stack');box.append(node('strong','직접 실행한 점수 '+direct.length+'개를 설정에 연결할 수 있어요.'));for(const r of direct.slice(0,3)){const row=node('div',null,'row between');row.append(node('span',scoreSummary(r)+' · '+date(r.createdAt)),button('설정 연결',()=>showLinkRun(r),'btn secondary tiny'));box.append(row);}target.append(box);}
    const history=node('div',null,'stack');
    sorted.forEach(r=>{const row=node('div',null,'row between'),info=node('div');info.append(node('strong',(r.binding==='unbound'?'연결되지 않은 기록':modeName(r.mode))+' · '+(r.status==='valid'?scoreSummary(r):'측정 실패')),node('p',date(r.createdAt)+(r.binding==='unbound'?' · 설정을 직접 연결할 수 있어요':r.binding==='manual'?' · 사용자가 설정을 연결했어요':''),'small muted'));row.append(info);if(r.status==='valid'&&r.binding!=='captured')row.append(button(r.binding==='manual'?'연결 수정':'설정 연결',()=>showLinkRun(r),'btn secondary tiny'));row.append(button('자세히',()=>showRun(r),'btn text'));history.append(row);});
    target.append(detail('전체 기록 보기 · '+runs.length+'회',history,'all-runs'));
  });
}
function showRun(r){const body=node('div',null,'stack');body.append(detailsList([['그래픽 점수',r.status==='valid'?number(r.graphicsScore)+'점':'정상 점수 없음'],['CPU 점수',r.status==='valid'?number(r.cpuScore)+'점':'정상 점수 없음'],['모드',r.binding==='unbound'?'당시 설정을 알 수 없음':modeName(r.mode)+(r.binding==='manual'?' · 직접 연결':'')],['측정 시각',date(r.createdAt)]]));const form=node('form');form.id='run-form';for(const [id,label,value,max,step]of [['noise','소음 (dBA)',r.noiseDbA,140,.1],['rpm','팬 속도 (RPM)',r.fanRpm,20000,1]]){const f=node('div',null,'field'),l=node('label',label);l.htmlFor=id;const input=node('input');Object.assign(input,{id,type:'number',min:0,max,step,value:value??'',placeholder:'측정한 값이 있을 때만 입력'});f.append(l,input);form.append(f);}const l=node('label','메모');l.htmlFor='run-notes';const notes=node('textarea');notes.id='run-notes';notes.maxLength=2000;notes.value=r.notes||'';notes.placeholder='예: 거치대 사용, 소음계 30cm 거리';form.append(l,notes,node('p','소음과 팬 속도는 직접 측정한 값을 적어 주세요.','small muted'));body.append(form,detail('기술 정보',node('pre',JSON.stringify({settingsHash:r.settingsHash,temperatures:r.temperatures,sourceFile:r.sourceFile},null,2),'code')));openModal('점수와 사용 환경',body,'기록 저장',async()=>{if(!form.reportValidity())return false;await api('/api/results/annotate',{id:r.id,noiseDbA:$('noise').value===''?null:Number($('noise').value),fanRpm:$('rpm').value===''?null:Number($('rpm').value),notes:notes.value});await refresh();showToast('기록을 저장했어요.');});form.addEventListener('submit',e=>{e.preventDefault();$('modal-confirm').click();});}
function showLinkRun(r){
  const profiles=(state.profiles||[]).filter(p=>p.origin==='local');
  if(!profiles.length){openModal('저장한 설정이 필요해요',node('p','측정 당시 사용한 G-Helper 설정이 저장되어 있어야 점수와 연결할 수 있습니다.'));return;}
  const body=node('div',null,'stack');
  body.append(notice(scoreSummary(r)+' · '+date(r.createdAt)+'에 가져온 결과입니다.'),node('p','측정 당시 사용한 저장 설정과 모드를 직접 선택해 주세요. 앱은 과거의 설정을 자동으로 검증할 수 없습니다.','small muted'));
  const form=node('form');
  const profileLabel=node('label','측정 당시 사용한 저장 설정'),profileSelect=node('select');profileLabel.htmlFor='link-profile';profileSelect.id='link-profile';profileSelect.required=true;
  const placeholder=node('option','저장 설정을 선택하세요');placeholder.value='';profileSelect.append(placeholder);
  for(const p of profiles){const option=node('option',p.name+' · '+date(p.createdAt)+' · '+modeName(p.activeMode));option.value=p.id;profileSelect.append(option);}
  profileSelect.value=profiles.some(p=>p.id===r.profileId)?r.profileId:'';
  const modeLabel=node('label','측정 당시 G-Helper 모드'),modeSelect=node('select');modeLabel.htmlFor='link-mode';modeSelect.id='link-mode';modeSelect.required=true;
  const modePlaceholder=node('option','모드를 선택하세요');modePlaceholder.value='';modeSelect.append(modePlaceholder);
  for(const mode of [2,0,1]){const option=node('option',modeName(mode));option.value=String(mode);modeSelect.append(option);}
  modeSelect.value=r.mode==null?'':String(r.mode);
  const checkLabel=node('label',null,'check-row'),check=node('input');check.type='checkbox';check.required=true;
  checkLabel.append(check,node('span','이 결과를 측정할 때 선택한 설정과 모드를 실제로 사용했습니다.'));
  form.append(profileLabel,profileSelect,modeLabel,modeSelect,checkLabel);
  body.append(form,notice('연결한 점수는 공유할 수 있으며, 공개 측정 메모에 「직접 연결 · 설정 미검증」이 표시됩니다.'));
  openModal('Time Spy 점수를 설정에 연결',body,'점수 연결',async()=>{
    if(!form.reportValidity())return false;
    await api('/api/results/link',{id:r.id,profileId:profileSelect.value,mode:Number(modeSelect.value),confirmed:check.checked});
    await refresh();showToast('점수를 설정에 연결했어요. 자료실에서 선택해 공유할 수 있습니다.');
  });
  form.addEventListener('submit',event=>{event.preventDefault();$('modal-confirm').click();});
}
function renderCommunity(){const profiles=state.profiles||[],select=$('public-profile'),selected=select.value||selectedPublic;const sig=JSON.stringify(profiles.map(p=>[p.id,p.name]));if(cache.publicProfiles!==sig){cache.publicProfiles=sig;select.replaceChildren();if(!profiles.length){const o=node('option','내 설정에서 먼저 저장해 주세요');o.value='';select.append(o);}else profiles.forEach(p=>{const o=node('option',p.name);o.value=p.id;select.append(o);});if(profiles.some(p=>p.id===selected))select.value=selected;selectedPublic=select.value;}renderPublicRuns();$('community-url-status').textContent='ghelper.optiwork.co.kr · 사용자 공유 기록';}
function eligible(p){return (state.runs||[]).filter(r=>p&&['captured','manual'].includes(r.binding)&&r.status==='valid'&&r.settingsHash===p.settingsHash);}
function renderPublicRuns(){const p=(state.profiles||[]).find(p=>p.id===$('public-profile').value),runs=eligible(p);publicSelection=new Set([...publicSelection].filter(id=>runs.some(r=>r.id===id)));memo('public-runs',[p?.id,runs],target=>{if(!runs.length){target.append(node('p','연결된 점수가 없어 설정만 공유합니다. 지난 결과는 성능 비교 화면에서 설정에 연결할 수 있어요.','small muted'));return;}for(const r of runs){const label=node('label',null,'check-row'),check=node('input');check.type='checkbox';check.checked=publicSelection.has(r.id);check.addEventListener('change',()=>check.checked?publicSelection.add(r.id):publicSelection.delete(r.id));label.append(check,node('span',modeName(r.mode)+' · '+scoreSummary(r)+' · '+date(r.createdAt)+(r.binding==='manual'?' · 직접 연결':'')));target.append(label);}});}
function renderCatalog(){
  const query=$('library-search').value.trim().toLocaleLowerCase(),tokens=query.split(/\s+/).filter(Boolean);
  const entries=(state.catalog?.entries||[]).filter(e=>{
    if(!tokens.length)return true;
    const value=[e.title,e.family,e.year,e.model,e.cpu,e.gpu,e.summary,e.settingsText,e.timeSpy?.graphics,e.timeSpy?.cpu,e.family?.replace('제피러스','Zephyrus')].filter(Boolean).join(' ').toLocaleLowerCase();
    const compact=value.replace(/[\s·_-]/g,'');
    return tokens.every(t=>value.includes(t)||compact.includes(t.replace(/[\s·_-]/g,'')));
  });
  $('catalog-count').textContent=entries.length+'개';
  $('catalog-more').hidden=entries.length<=catalogVisible;
  memo('catalog-list',[query,catalogVisible,entries.slice(0,catalogVisible)],target=>{
    if(!entries.length){target.append(notice('일치하는 참고 자료가 없어요. 모델 코드나 RTX 그래픽카드로 다시 검색해 보세요.'));return;}
    for(const e of entries.slice(0,catalogVisible)){
      const card=node('article',null,'source-card');
      card.append(node('strong',(e.sourceType==='official'?'ASUS 공식 사양 · ':e.timeSpy?'사용자 Time Spy · ':'참고 기록 · ')+(e.title||e.model)),node('p',[e.cpu,e.gpu].filter(Boolean).join(' · '),'small muted'),node('p',e.summary||e.notes||'','small muted'));
      if(e.timeSpy)card.append(referenceScore(e.timeSpy));
      const content=node('div',null,'stack');
      if(e.settingsText)content.append(node('p',e.settingsText));
      if(e.timeSpy?.conditions)content.append(node('p','측정 조건: '+e.timeSpy.conditions));
      if(e.limitations)content.append(notice(e.limitations));
      const url=safeUrl(e.url||e.sourceUrl||e.source?.url);
      if(url){const a=node('a','출처 원문 보기 ↗');a.href=url;a.target='_blank';a.rel='noopener noreferrer';content.append(a);}
      card.append(detail('설정 조건과 출처',content,e.id));target.append(card);
    }
  });
}
function renderSettings(){const h=state.hardware,d=state.discovery;$('hardware-details').replaceChildren(...detailsList([['노트북',h.model],['CPU',h.cpu],['그래픽카드',Array.isArray(h.gpu)?h.gpu.join(' / '):h.gpu],['메모리',number(h.ram_gb)+' GB'],['BIOS',h.bios]]).childNodes);$('connection-details').replaceChildren(...detailsList([['G-Helper',d.ghelper_running?'실행 중':'종료됨'],['버전',d.ghelper_version],['3DMark',d.benchmark_exe?'설치 확인됨':'확인 필요']]).childNodes);const options=$('config-candidates');if(cache.configCandidates!==JSON.stringify(d.config_candidates)){cache.configCandidates=JSON.stringify(d.config_candidates);options.replaceChildren();const o=node('option','직접 경로 입력');o.value='';options.append(o);for(const path of d.config_candidates||[]){const opt=node('option',path);opt.value=path;options.append(opt);}}if(!$('config-path').dataset.dirty&&document.activeElement!==$('config-path'))$('config-path').value=d.config_path||'';$('current-settings').textContent=JSON.stringify(state.config,null,2);}
function openModal(title,body,confirmText='',action=null){if(!$('modal').open)lastFocus=document.activeElement;dialog={id:++dialogId,action,pending:false};$('modal-title').textContent=title;$('modal-body').replaceChildren(body);$('modal-confirm').hidden=!action;$('modal-confirm').textContent=confirmText;$('modal-confirm').disabled=false;$('modal-cancel').textContent=action?'취소':'닫기';$('modal-cancel').disabled=false;$('modal-close').disabled=false;if(!$('modal').open)$('modal').showModal();}
let lastFocus=null;
function closeModal(){if(dialog?.pending)return;$('modal').close();dialog=null;if(lastFocus?.isConnected)lastFocus.focus();}
async function confirmModal(){const current=dialog;if(!current?.action||current.pending)return;current.pending=true;const label=$('modal-confirm').textContent;$('modal-confirm').disabled=true;$('modal-confirm').textContent='처리 중…';$('modal-cancel').disabled=true;$('modal-close').disabled=true;try{const result=await current.action();if(dialog?.id===current.id){current.pending=false;if(result!==false)closeModal();}}catch(e){if(dialog?.id===current.id)$('modal-body').prepend(notice(e.message||String(e),'error'));else report(e);}finally{current.pending=false;if(dialog?.id===current.id){$('modal-confirm').textContent=label;$('modal-confirm').disabled=false;$('modal-cancel').disabled=false;$('modal-close').disabled=false;}}}
function fileName(v){return String(v||'설정').replace(/[<>:"/\\|?*\x00-\x1f]/g,'_').slice(0,100);}
function download(blob,name){const url=URL.createObjectURL(blob),a=node('a');a.href=url;a.download=name;document.body.append(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),60000);showToast('저장 창에서 파일을 저장해 주세요.');}
$('quick-save').addEventListener('click',e=>act(e.currentTarget,()=>saveCurrent()));
$('capture-form').addEventListener('submit',e=>{e.preventDefault();act(e.submitter,async()=>{await saveCurrent($('profile-name').value,$('profile-notes').value);e.target.reset();e.target.closest('details').open=false;});});
$('import-button').addEventListener('click',()=>$('profile-file').click());
$('profile-file').addEventListener('change',async()=>{const file=$('profile-file').files[0];if(!file)return;await act($('import-button'),async()=>{if(file.size>1024*1024)throw Error('1 MB 이하의 설정 파일을 선택해 주세요.');let data;try{data=JSON.parse(await file.text());}catch{throw Error('설정 파일을 읽을 수 없어요. Profile Lab에서 저장한 파일을 선택해 주세요.');}await api('/api/profiles/import',{profile:data});await refresh();showToast('설정을 가져왔어요. 목록에서 「적용하기」를 누르면 됩니다.');});$('profile-file').value='';});
$('refresh-button').addEventListener('click',e=>act(e.currentTarget,()=>refresh(true)));
$('update-button').addEventListener('click',()=>checkForUpdates(true));
$('settings-button').addEventListener('click',()=>$('settings-modal').showModal());$('settings-close').addEventListener('click',()=>$('settings-modal').close());
$('config-path').addEventListener('input',()=>$('config-path').dataset.dirty='true');$('config-candidates').addEventListener('change',()=>{if($('config-candidates').value){$('config-path').value=$('config-candidates').value;$('config-path').dataset.dirty='true';}});
$('config-form').addEventListener('submit',e=>{e.preventDefault();act(e.submitter,async()=>{accept(await api('/api/config',{path:$('config-path').value.trim()}));delete $('config-path').dataset.dirty;showToast('G-Helper 설정을 찾았어요.');});});
$('prepare-again').addEventListener('click',()=>{setupExpanded=!setupExpanded;renderBenchmark();});$('start-benchmark').addEventListener('click',startBenchmark);document.querySelectorAll('input[name=driver]').forEach(e=>e.addEventListener('change',()=>{renderBenchmark();renderAvailability();}));
$('cancel-benchmark').addEventListener('click',e=>act(e.currentTarget,async()=>{state.benchmark=await api('/api/benchmark/cancel',{});renderBenchmark();showToast('중단을 요청했어요. 원래 모드로 돌아갈 때까지 기다려 주세요.');schedule();}));
$('scan-results').addEventListener('click',e=>act(e.currentTarget,async()=>{const r=await api('/api/results/scan',{});await refresh();showToast('지난 기록 '+(r.added||0)+'개를 가져왔어요. 전체 기록에서 확인하세요.');}));
$('catalog-more').addEventListener('click',()=>{catalogVisible+=18;renderCatalog();});
$('public-profile').addEventListener('change',()=>{selectedPublic=$('public-profile').value;publicSelection.clear();cache['public-runs']=null;renderPublicRuns();});
$('community-form').addEventListener('submit',e=>{e.preventDefault();act(e.submitter,prepareShare);});
$('shutdown-button').addEventListener('click',()=>{const body=node('div',null,'stack');if(isActive()){body.append(node('p','비교를 먼저 중단하고 원래 모드로 돌아온 뒤 종료해 주세요.'));openModal('성능 비교가 진행 중이에요',body);return;}body.append(node('p','트레이 아이콘까지 완전히 종료합니다. 창만 숨기려면 × 버튼을 누르세요.'));openModal('프로그램을 종료할까요?',body,'종료',async()=>{await api('/api/shutdown',{});stopped=true;clearTimeout(pollTimer);$('app-content').hidden=true;$('connection-error').hidden=false;$('connection-error').textContent='프로그램을 종료했습니다.';});});
$('modal-confirm').addEventListener('click',confirmModal);$('modal-cancel').addEventListener('click',closeModal);$('modal-close').addEventListener('click',closeModal);$('modal').addEventListener('cancel',e=>{e.preventDefault();closeModal();});$('toast-close').addEventListener('click',()=>$('toast').hidden=true);
window.addEventListener('hashchange',navigate);window.addEventListener('beforeunload',()=>clearTimeout(pollTimer));setInterval(renderCountdown,1000);


let currentVisual=null,currentVisualSignature='',currentViewMode=null;
function renderCurrentVisual(){
  const h=state.hardware; const gpu=Array.isArray(h.gpu)?h.gpu.find(v=>!/Intel.*Graphics|Intel.*Arc/i.test(v))||h.gpu[0]:h.gpu;
  $('hardware-summary').textContent=[h.cpu,gpu,number(h.ram_gb)+' GB'].filter(Boolean).join(' · ');
  const signature=JSON.stringify([state.config,h]);if(signature===currentVisualSignature)return;
  currentVisualSignature=signature;currentVisual?.destroy();
  currentVisual=ProfileSettings.render($('current-visual'),state.config,h,{mode:currentViewMode??state.config.performance_mode??0,onModeChange:m=>currentViewMode=m});
}
function showSavedSettings(p){const body=node('div',null,'stack');body.append(node('p',p.hardware.model,'small muted'));const visual=node('div');body.append(visual);ProfileSettings.render(visual,p.settings,p.hardware,{mode:p.activeMode??0});openModal(p.name,body,sameSettings(p)?'':'이 설정 적용',sameSettings(p)?null:async()=>{await openApply(p);return false;});}
